<?php
$response = $this->getResponse();

$country = \App\Router::$lang === 'en' ? 'GB' : \App\Router::$lang;

$this->localizeUi(array('auto_service', 'spare_parts', 'automobiles', 'repair_cards', 'exit'));
?>

<nav class="navbar navbar-expand-lg navbar-toggleable-md navbar-dark bg-primary">
  <button class="navbar-toggler navbar-toggler-right"
          type="button"
          data-toggle="collapse"
          data-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <span class="navbar-brand text-capitalize"><?= $this->getUiLocaleField('auto_service'); ?></span>

  <div class="collapse navbar-collapse" id="navbarSupportedContent" role="tabpanel">
    <ul class="autoservice-navigation navbar-nav nav mr-auto" role="tablist">
      <li class="nav-item ">
        <a class="nav-link text-capitalize <?php if ($response['params']['params'] == \App\Router::$lang.'/AutomobilePart') echo 'active';?>" href="<?= ROUTER_URL_NOPROTOCOL . '/AutomobilePart'; ?>"
        ><?= $this->getUiLocaleField('spare_parts'); ?></a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-capitalize <?php if ($response['params']['params'] == \App\Router::$lang.'/AutomobileData') echo 'active';?>" href="<?= ROUTER_URL_NOPROTOCOL . '/AutomobileData'; ?>"
        ><?= $this->getUiLocaleField('automobiles'); ?></a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-capitalize <?php if ($response['params']['params'] == \App\Router::$lang.'/RepairCardData') echo 'active';?>" href="<?= ROUTER_URL_NOPROTOCOL . '/RepairCardData'; ?>"
        ><?= $this->getUiLocaleField('repair_cards'); ?></a>
      </li>
    </ul>
      <div class="my-2 mr-5" >
          <div id="options" data-input-name="country" data-selected-country="<?= $country; ?>"></div>
      </div>
    <a class="btn text-capitalize btn-danger my-2 my-sm-0 " href="<?= ROUTER_URL_NOPROTOCOL . '/Logout'; ?>"><?= $this->getUiLocaleField('exit'); ?></a>
  </div>
</nav>

<script src="<?= OPERATOR_URL_NOPROTOCOL; ?>/public/js/Navbar.js"></script>